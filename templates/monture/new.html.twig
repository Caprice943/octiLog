{% extends 'base.html.twig' %}

{% block title %}Nouvelle monture{% endblock %}

{% block body %}
    <a href="{{ path('app_monture_index') }}" class="mb-6 inline-block text-sm font-medium text-slate-500 hover:text-slate-700">&larr; Retour à la liste</a>

    <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
        <h1 class="mb-6 text-xl font-semibold text-slate-900">Ajouter une monture</h1>

        {{ include('monture/_form.html.twig') }}
    </div>
{% endblock %}
